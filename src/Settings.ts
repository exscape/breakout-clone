export type Settings = {
    canvasWidth: number,
    canvasHeight: number,
    canvasMargin: number,
    ballRadius: number,
    paddleThickness: number,
    ballSpeed: number,
    brickWidth: number
    brickHeight: number
};